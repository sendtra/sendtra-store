-- Generated by Sendtra Widget Studio -- do not hand-edit,
-- re-export from the designer instead so the two stay in sync.
-- Widget: Siaa

function draw(ox, oy, w, h)
  do
    local dx, dy = 0.0000, 1.0000
    local halfW, halfH = w / 2, h / 2
    local projMax = math.abs(halfW * dx) + math.abs(halfH * dy)
    if projMax < 0.001 then projMax = 1 end
    for by = 0, h - 1, 6 do
      local bh = math.min(6, h - by)
      for bx = 0, w - 1, 6 do
        local bw = math.min(6, w - bx)
        local proj = (bx + bw / 2 - halfW) * dx + (by + bh / 2 - halfH) * dy
        local t = 0.5 + proj / (2 * projMax)
        if t < 0 then t = 0 elseif t > 1 then t = 1 end
        fill_rect(ox + bx, oy + by, bw, bh, blend_color(0xF65F, 0xFDB5, t))
      end
    end
  end
  draw_ring(ox + 166, oy + 26, 20, 15, 0.65, 0xFFFF, 0x2104, 0x0000)
  draw_text(ox + 81, oy + 16, "Siaa", 0xFFFF)
  draw_text_bold(ox + 12, oy + 6, string.format("%02d:%02d", get_hour(), get_minute()), 0xFFFF)
end
